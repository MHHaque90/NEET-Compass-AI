# NEET Compass AI - Development Dependencies
# Extra dependencies for testing and development

# Runtime deps (from requirements.txt)
pydantic-settings >= 2.7.0
PyYAML >= 6.0
openpyxl >= 3.0.0  # test: Excel source reading
psycopg >= 3.0.0  # runtime: PostgreSQL driver
SQLAlchemy >= 2.0.0
alembic >= 1.10.0
pytest >= 7.0.0
ruff >= 0.1.0
mypy >= 1.0.0
black >= 23.0.0

# Test infrastructure
pytest-cov >= 4.0.0   # Coverage integration for pytest
pytest-mock >= 3.0.0  # Mocking support
freezegun >= 1.2.0     # Time freezing for tests
coverage >= 7.0.0      # Coverage reporting

# Type stubs
types-pydantic >= 0.25.0
types-sqlalchemy >= 2.0.0