# Frontend

Placeholder — no frontend is built in Phase 1 by design (see
[`docs/ARCHITECTURE.md`](../docs/ARCHITECTURE.md#2-scope-discipline)).

Phase 3 will add a client (choice-filling UI) that consumes the backend API.
Planned tech: TypeScript + React (Vite). This directory will then contain the
application, and `CORS_ORIGINS` in `.env` will be set to the client origin.
